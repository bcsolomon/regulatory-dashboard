"""\nRegulatory Dashboard Backend API\nFlask server that connects to Teradata and provides REST endpoints\n\nTERADATA SQL NOTES:\n- Database: hcls\n- Key views: V_Regulatory_Dashboard, V_Intervention_Queue, V_ROI_Calculator,\n             V_Portfolio_Summary, V_Critical_Delays, V_Approval_Pipeline\n- Key tables: Product_Registration, Activity_Country, Submission_Risk_Features\n- Date arithmetic: Use CURRENT_DATE - N (not ADD_DAYS)\n- Parameterized queries: Use ? for placeholders with cursor.execute(query, params)\n\nAI RECOMMENDATION NOTES:\n- AWS Bedrock client uses env vars: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY,\n  AWS_SESSION_TOKEN (STS only), AWS_DEFAULT_REGION, BEDROCK_MODEL_ID\n- Model: us.anthropic.claude-sonnet-4-5-20251001\n- Endpoint: POST /api/ai/intervention-recommendation\n- Guardrails: FORBIDDEN_KEYWORDS blocklist + system prompt scoping\n- Allowed HCLS objects: V_Intervention_Queue, V_Critical_Delays, V_Approval_Pipeline,\n  V_ROI_Calculator, V_Regulatory_Dashboard, Activity_Country,\n  Product_Registration, Submission_Risk_Features\n"""\n\nimport boto3\nimport json\nfrom flask import Flask, jsonify, request\nfrom flask_cors import CORS\nimport teradatasql\nfrom datetime import datetime\nimport os\nfrom functools import wraps\nfrom dotenv import load_dotenv\n\n# Load environment variables from .env file\nload_dotenv()\n\napp = Flask(__name__)\nCORS(app)  # Enable CORS for React frontend\n\n# ============================================\n# TERADATA CONNECTION CONFIGURATION\n# ============================================\n\nTERADATA_CONFIG = {\n    'host': os.getenv('TERADATA_HOST', 'your-teradata-host'),\n    'user': os.getenv('TERADATA_USER', 'your-username'),\n    'password': os.getenv('TERADATA_PASSWORD', 'your-password'),\n}\n\ndef get_teradata_connection():\n    \"\"\"Create Teradata connection\"\"\"\n    try:\n        conn = teradatasql.connect(\n            host=TERADATA_CONFIG['host'],\n            user=TERADATA_CONFIG['user'],\n            password=TERADATA_CONFIG['password']\n        )\n        return conn\n    except Exception as e:\n        print(f\"Error connecting to Teradata: {e}\")\n        raise\n\ndef execute_query(query, params=None):\n    \"\"\"Execute Teradata query and return results\"\"\"\n    try:\n        with get_teradata_connection() as conn:\n            cursor = conn.cursor()\n            if params:\n                cursor.execute(query, params)\n            else:\n                cursor.execute(query)\n\n            columns = [desc[0] for desc in cursor.description]\n            results = cursor.fetchall()\n\n            return [dict(zip(columns, row)) for row in results]\n    except Exception as e:\n        print(f\"Query error: {e}\")\n        raise\n\n# Error handler decorator\ndef handle_errors(f):\n    @wraps(f)\n    def decorated_function(*args, **kwargs):\n        try:\n            return f(*args, **kwargs)\n        except Exception as e:\n            return jsonify({'error': str(e)}), 500\n    return decorated_function\n\n\n# ============================================\n# BEDROCK CONFIGURATION\n# ============================================\n\nBEDROCK_CONFIG = {\n    'region':   os.getenv('AWS_DEFAULT_REGION', 'us-east-1'),\n    'model_id': os.getenv('BEDROCK_MODEL_ID', 'us.anthropic.claude-sonnet-4-5-20251001'),\n}\n\ndef get_bedrock_client():\n    \"\"\"\n    Create a boto3 Bedrock Runtime client.\n    Credentials loaded automatically from environment variables:\n      AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_SESSION_TOKEN (STS/temp keys)\n    \"\"\"\n    return boto3.client(\n        service_name='bedrock-runtime',\n        region_name=BEDROCK_CONFIG['region'],\n    )\n\n\n# ============================================\n# AI GUARDRAIL VALIDATION\n# ============================================\n\nFORBIDDEN_KEYWORDS = [\n    'DROP', 'DELETE', 'INSERT', 'UPDATE', 'TRUNCATE', 'ALTER', 'CREATE',\n    'GRANT', 'REVOKE', 'EXEC', 'EXECUTE', 'MERGE',\n    'DBC.', 'DBQL', 'DBQLOG',\n    'PAID_CLAIMS', 'DENIED_CLAIMS',\n    'INPATIENT_HDR', 'INPATIENT_DTL',\n    'WORKERS_COMP_HDR', 'WORKERS_COMP_DTL',\n    'PHYSICIAN_CONTRACT', 'PHYSICIAN_CHUNKS',\n    'ICD_EMBEDDINGS', 'CPT_EMBEDDINGS', 'DRG_EMBEDDINGS',\n    'POS_EMBEDDINGS', 'CARC_EMBEDDINGS',\n    'USER_QUERY_EMBEDDINGS', 'CLAIM_QUESTIONS',\n]\n\ndef validate_ai_input(text: str) -> tuple[bool, str]:\n    \"\"\"\n    Returns (is_valid, reason).\n    Defense-in-depth check — system prompt handles primary guardrailing.\n    \"\"\"\n    upper = text.upper()\n    for kw in FORBIDDEN_KEYWORDS:\n        if kw in upper:\n            return False, f\"Input contains restricted keyword: {kw}\"\n    return True, \"\"\n\n\n# ============================================\n# AI SYSTEM PROMPT (loaded once at module level)\n# ============================================\n\nINTERVENTION_SYSTEM_PROMPT = \"\"\"\nYou are a pharmaceutical regulatory affairs AI advisor embedded in a drug submission\ntracking dashboard. Your role is to analyze active intervention queue items and provide\nspecific, actionable recommendations to regulatory operations teams.\n\n=== YOUR SCOPE AND GUARDRAILS ===\n\nYou operate exclusively against the following Teradata HCLS database objects:\n  ALLOWED VIEWS (read-only):\n    - hcls.V_Intervention_Queue       — primary source: prioritized items needing action\n    - hcls.V_Critical_Delays          — submissions critically overdue with delay drivers\n    - hcls.V_Approval_Pipeline        — pipeline breakdown by region/sector/pathway\n    - hcls.V_ROI_Calculator           — revenue at risk and acceleration scenarios\n    - hcls.V_Regulatory_Dashboard     — portfolio-level KPI summary\n\n  ALLOWED BASE TABLES (read-only):\n    - hcls.Activity_Country           — submission-level regulatory activity per country\n    - hcls.Product_Registration       — product master with sales potential and approval status\n    - hcls.Submission_Risk_Features   — risk scoring: CMC, GMP, clinical data gaps\n\n  FORBIDDEN (never query or reference these):\n    - Any table NOT listed above (claims, embeddings, workers comp, ICD/CPT/DRG tables)\n    - Any DDL: CREATE, ALTER, DROP, TRUNCATE\n    - Any DML: INSERT, UPDATE, DELETE, MERGE\n    - Any GRANT, REVOKE, or permission changes\n    - Any cross-database queries outside the hcls schema\n    - DBC system tables or any metadata catalog tables\n\nIf asked to operate outside these boundaries, refuse and explain the restriction.\n\n=== PHARMACEUTICAL REGULATORY DOMAIN CONTEXT ===\n\nREGULATORY AUTHORITIES AND TIMELINES:\n- FDA (USA): Standard NDA/BLA review = 12 months under PDUFA. Priority Review = 6 months.\n  Complete Response Letters (CRLs) are issued when FDA cannot approve in the current cycle.\n  Each CRL resets the review clock. Resubmission is Class 1 (2-month review) or\n  Class 2 (6-month review) depending on scope of changes.\n- EMA (EU): Centralized procedure = 210 active review days, excluding clock stops.\n  Day 120/180 List of Outstanding Issues (LOIs) require formal sponsor responses.\n- PMDA (Japan): Standard review = 12 months. Priority = 6 months. Bridging studies\n  often required for ethnic sensitivity data. Japan-specific stability data may be needed.\n- Other authorities (Health Canada, TGA, ANVISA, MHRA) follow similar CTD-based models.\n\nCTD/eCTD DOSSIER MODULES:\n- Module 1: Regional administrative info (country-specific)\n- Module 2: Summaries of Modules 3, 4, 5\n- Module 3: CMC — Chemistry, Manufacturing, and Controls\n- Module 4: Nonclinical study reports\n- Module 5: Clinical study reports\n\nCOMPLETE RESPONSE LETTER (CRL):\n- Issued by FDA when a submission cannot be approved as filed.\n- CRL Categories: Clinical (efficacy/safety gaps), CMC (manufacturing deficiencies),\n  Labeling (unresolved label text), Facility (GMP/inspection failures),\n  Multiple (combination of above).\n- Commercial impact: each day of delay costs ~1/365 of peak annual sales.\n  A $1B peak sales product loses ~$2.74M per day.\n- \"Multiple\" CRL category = highest complexity resubmission, typically Class 2 (6 months).\n\nCMC (CHEMISTRY, MANUFACTURING, CONTROLS):\n- Module 3 of the CTD dossier covering drug substance, drug product, and manufacturing.\n- CMC_Readiness_Score interpretation: 1-3 = HIGH RISK, 4-6 = MODERATE, 7-10 = LOW RISK.\n- Common CMC risks: incomplete stability data, manufacturing site changes, analytical\n  method validation gaps, container-closure system issues, impurity profile concerns.\n\nGMP SITE STATUS:\n- \"Cleared\": site passed inspection — no manufacturing blocker.\n- \"Pending Inspection\": inspection scheduled or awaited — approval-blocking risk;\n  FDA/EMA will not approve until inspection is complete and passed.\n- \"Warning Letter Active\": serious GMP compliance violation — approval very unlikely\n  until fully resolved. Warning letters typically take 12-24 months to close.\n  Requires immediate escalation to VP Manufacturing and Quality.\n- \"Import Alert\": products from this site detained at US border — severe blocker.\n\nCLOCK STOPS:\n- Review clock pauses while awaiting sponsor responses to agency information requests.\n- Clock_Stop_Days = cumulative days not counted toward review timeline.\n- Excessive clock stops (>90 days) indicate fundamental dossier deficiencies.\n- Each clock stop response must be comprehensive — incomplete responses restart the stop.\n\nOPEN DEFICIENCIES:\n- Numbered outstanding issues requiring formal written sponsor responses.\n- 1-2 open: manageable; 3-5: high burden; 6+: critical — suggests systemic dossier issues.\n- Each deficiency requires a formal response typically within 30-90 days of issuance.\n\nPRIORITY SCORE THRESHOLDS (this system):\n- >100: CRITICAL — immediate escalation and executive awareness required\n- 75-100: HIGH — action required within 1-2 weeks\n- 50-74: MODERATE — active monitoring, response plan must be in place\n- <50: WATCH — standard follow-up sufficient\n\n=== RESPONSE FORMAT ===\n\nAlways respond with valid JSON only. No preamble, no markdown, no explanation outside the JSON.\n\n{\n  \"recommendation_summary\": \"<2-3 sentence executive summary of the situation and core risk>\",\n  \"urgency_level\": \"<CRITICAL | HIGH | MODERATE | WATCH>\",\n  \"next_actions\": [\n    {\n      \"action\": \"<specific action — name the dossier module, agency, or regulatory pathway>\",\n      \"owner\": \"<RA Team | CMC Team | Manufacturing Quality | Clinical | Executive | Agency Liaison>\",\n      \"timeframe\": \"<Immediate (24-48h) | This Week | Within 30 Days | Within 90 Days>\"\n    }\n  ],\n  \"timeline_impact\": {\n    \"current_delay_days\": <integer>,\n    \"estimated_revenue_at_risk_per_day_usd\": <float>,\n    \"total_revenue_at_risk_usd\": <float>,\n    \"acceleration_opportunity\": \"<what happens commercially if action is taken vs. not taken in next 90 days>\"\n  },\n  \"regulatory_context\": \"<1-2 sentences on the specific regulatory pathway or agency precedent relevant to this submission>\",\n  \"confidence\": \"<HIGH | MEDIUM | LOW>\",\n  \"confidence_note\": \"<brief note on what additional data would improve this recommendation>\"\n}\n\nKeep next_actions to 3-5 items. Be specific — reference the regulatory pathway, agency,\nand dossier module where applicable. Avoid generic advice without specifying outcomes.\n\"\"\"\n\n\n# ============================================\n# AI PROMPT ASSEMBLY\n# ============================================\n\ndef build_intervention_user_prompt(item: dict, portfolio_context: list) -> str:\n    \"\"\"Build the user-turn prompt, injecting live Teradata snapshot data.\"\"\"\n    crl_received = str(item.get('CRL_Received_Fl', '0'))\n    crl_category = item.get('CRL_Category', 'None')\n    crl_note = f\"YES (Category: {crl_category})\" if crl_received == '1' else \"No\"\n\n    revenue_millions = float(item.get('Revenue_At_Risk_Millions') or 0)\n    revenue_usd = revenue_millions * 1_000_000\n    revenue_per_day = round(revenue_usd / 365, 0)\n\n    filtered_portfolio = [\n        {\n            'Protocol_ID':        r.get('Protocol_ID'),\n            'Product':            r.get('Product'),\n            'Priority_Score':     r.get('Priority_Score'),\n            'Risk_Score':         r.get('Risk_Score'),\n            'Days_Overdue':       r.get('Days_Overdue'),\n            'Regulatory_Authority': r.get('Regulatory_Authority'),\n        }\n        for r in portfolio_context\n        if r.get('Protocol_ID') != item.get('Protocol_ID')\n    ][:5]\n\n    return f\"\"\"Analyze the following intervention queue item and provide a regulatory recommendation.\n\nSUBMISSION DATA:\n- Protocol ID: {item.get('Protocol_ID', 'N/A')}\n- Product: {item.get('Product', 'N/A')}\n- Regulatory Authority: {item.get('Regulatory_Authority', 'N/A')}\n- Country Code: {item.get('Country_Code', 'N/A')}\n- Sector: {item.get('Sector', 'N/A')}\n- Priority Score: {item.get('Priority_Score', 'N/A')}\n- Risk Score (1-10): {item.get('Risk_Score', 'N/A')}\n- Days Overdue: {item.get('Days_Overdue', 'N/A')}\n- CRL Received: {crl_note}\n- GMP Site Status: {item.get('GMP_Site_Status', 'N/A')}\n- CMC Readiness Score (1-10): {item.get('CMC_Readiness_Score', 'N/A')}\n- Dose Optimization Status: {item.get('Dose_Optimization_Status', 'N/A')}\n- Open Deficiencies Count: {item.get('Open_Deficiencies_Cnt', 'N/A')}\n- Peak Sales Potential: ${revenue_millions:.1f}M\n- Revenue at Risk Per Day: ${revenue_per_day:,.0f}\n- Current System Recommendation: {item.get('Recommended_Action', 'N/A')}\n\nPORTFOLIO CONTEXT (other top items in intervention queue for relative urgency):\n{json.dumps(filtered_portfolio, indent=2)}\n\nBased on this data:\n1. What specific actions should the regulatory team take NOW?\n2. What is the commercial impact if no action is taken in the next 30/60/90 days?\n3. Are there any GMP, CMC, or agency-specific factors that change the urgency?\n\nRespond only with the JSON format specified in your instructions.\n\"\"\"\n\n\n# ============================================\n# API ENDPOINTS\n# ============================================\n\n@app.route('/api/health', methods=['GET'])\ndef health_check():\n    \"\"\"Health check endpoint\"\"\"\n    return jsonify({'status': 'healthy', 'timestamp': datetime.now().isoformat()})\n\n\n@app.route('/api/kpis', methods=['GET'])\n@handle_errors\ndef get_kpis():\n    region = request.args.get('region', 'all')\n    sector = request.args.get('sector', 'all')\n    status = request.args.get('status', 'all')\n    days   = int(request.args.get('days', 9999))\n\n    where_conditions = [\"1=1\"]\n    if region != 'all':\n        where_conditions.append(f\"ac.Global_Region = '{region}'\")\n    if sector != 'all':\n        where_conditions.append(f\"pr.Actv_Sectr = '{sector}'\")\n    if status != 'all':\n        where_conditions.append(f\"ac.Actv_Ctry_Sts = '{status}'\")\n    where_clause = \" AND \".join(where_conditions)\n\n    query = f\"\"\"\n    SELECT\n        COUNT(*)                                                              AS total_submissions,\n        SUM(CASE WHEN ac.Helth_Auth_Init_Sbmn_Actl >= CURRENT_DATE - {days}\n                 THEN pr.Peak_Sales_Potential ELSE 0 END) / 1000000000.0     AS total_pipeline_billions,\n        COUNT(CASE WHEN ac.Actv_Ctry_Sts NOT IN ('Approved','Withdrawn')\n                    AND ac.Helth_Auth_Init_Sbmn_Actl >= CURRENT_DATE - {days}\n              THEN 1 END)                                                    AS pending_approvals,\n        COUNT(CASE WHEN ac.Actv_Ctry_Sts = 'Approved'\n                    AND ac.Helth_Auth_Init_Appr_Actl IS NOT NULL\n                    AND ac.Helth_Auth_Init_Appr_Actl >= CURRENT_DATE - {days}\n               THEN 1 END)                                                   AS approved_ytd,\n        AVG(CASE WHEN ac.Actv_Ctry_Sts = 'Approved'\n                  AND ac.Helth_Auth_Init_Appr_Actl IS NOT NULL\n                  AND ac.Helth_Auth_Init_Sbmn_Actl IS NOT NULL\n                  AND ac.Helth_Auth_Init_Appr_Actl >= CURRENT_DATE - {days}\n             THEN CAST((ac.Helth_Auth_Init_Appr_Actl - ac.Helth_Auth_Init_Sbmn_Actl) AS INTEGER)\n             END)                                                             AS avg_cycle_time,\n        COUNT(CASE WHEN ac.CRL_Received_Fl = 1\n                    AND ac.Helth_Auth_Init_Sbmn_Actl >= CURRENT_DATE - {days}\n               THEN 1 END)                                                   AS crl_count\n    FROM hcls.Activity_Country ac\n    JOIN hcls.Product_Registration pr\n        ON ac.Actv_Id = pr.Actv_Id\n       AND TRIM(ac.Ctry_Cd_Iso3) = TRIM(pr.Ctry_Cd_Iso3)\n    WHERE {where_clause}\n    \"\"\"\n\n    results = execute_query(query)\n    if results:\n        kpi = results[0]\n        return jsonify({\n            'totalSubmissions': int(kpi['total_submissions'] or 0),\n            'totalPipeline':    round(float(kpi['total_pipeline_billions'] or 0), 1),\n            'pendingApprovals': int(kpi['pending_approvals'] or 0),\n            'approvedYTD':      int(kpi['approved_ytd'] or 0),\n            'avgCycleTime':     round(float(kpi['avg_cycle_time'] or 0), 0),\n            'crlCount':         int(kpi['crl_count'] or 0),\n        })\n\n    return jsonify({'error': 'No data found'}), 404\n\n\n@app.route('/api/submissions', methods=['GET'])\n@handle_errors\ndef get_submissions():\n    region = request.args.get('region', 'all')\n    sector = request.args.get('sector', 'all')\n    status = request.args.get('status', 'all')\n    days = int(request.args.get('days', 9999))\n\n    where_conditions = [\"1=1\"]\n    if region != 'all':\n        where_conditions.append(f\"ac.Global_Region = '{region}'\")\n    if sector != 'all':\n        where_conditions.append(f\"pr.Actv_Sectr = '{sector}'\")\n    if status != 'all':\n        where_conditions.append(f\"ac.Actv_Ctry_Sts = '{status}'\")\n    where_conditions.append(\n        f\"(ac.Helth_Auth_Init_Sbmn_Actl >= CURRENT_DATE - {days}\"\n        f\" OR (ac.Actv_Ctry_Sts = 'Approved' AND ac.Helth_Auth_Init_Appr_Actl >= CURRENT_DATE - {days}))\"\n    )\n    where_clause = \" AND \".join(where_conditions)\n\n    query = f\"\"\"\n    SELECT\n        ac.Actv_Id                       AS Protocol_ID,\n        pr.Prod_Brnd_Nm                  AS Product,\n        pr.Prod_Typ                      AS Product_Type,\n        ac.Ctry_Name                     AS Country,\n        ac.Ctry_Cd_Iso3                  AS Country_Code,\n        ac.Global_Region,\n        pr.Actv_Sectr                    AS Sector,\n        ac.Regulatory_Authority,\n        ac.Helth_Auth_Init_Sbmn_Actl     AS Submission_Date,\n        ac.Actv_Ctry_Sts                 AS Submission_Status,\n        ac.Helth_Auth_Init_Appr_Exptd    AS Expected_Approval_Date,\n        ac.Helth_Auth_Init_Appr_Actl     AS Actual_Approval_Date,\n        ac.CRL_Received_Fl,\n        ac.CRL_Category,\n        ac.Clock_Stop_Days,\n        CASE\n            WHEN ac.Actv_Ctry_Sts = 'Approved' AND ac.Helth_Auth_Init_Appr_Actl IS NOT NULL\n                THEN CAST((ac.Helth_Auth_Init_Appr_Actl - ac.Helth_Auth_Init_Sbmn_Actl) AS INTEGER)\n            ELSE CAST((CURRENT_DATE - ac.Helth_Auth_Init_Sbmn_Actl) AS INTEGER)\n        END AS Days_In_Review,\n        CASE\n            WHEN ac.Actv_Ctry_Sts <> 'Approved' AND ac.Helth_Auth_Init_Appr_Exptd IS NOT NULL\n                THEN CAST((CURRENT_DATE - ac.Helth_Auth_Init_Appr_Exptd) AS INTEGER)\n            ELSE 0\n        END AS Days_Overdue,\n        ac.Review_Pathway,\n        pr.Peak_Sales_Potential / 1000000.0 AS Peak_Sales_Millions,\n        pr.Rglt_Appr_By_Ctry             AS Regulatory_Approval_Status,\n        pr.Mktg_Sts_By_Ctry              AS Marketing_Status,\n        srf.Risk_Score_Overall           AS Risk_Score\n    FROM hcls.Activity_Country ac\n    JOIN hcls.Product_Registration pr\n        ON ac.Actv_Id = pr.Actv_Id\n       AND TRIM(ac.Ctry_Cd_Iso3) = TRIM(pr.Ctry_Cd_Iso3)\n    LEFT JOIN hcls.Submission_Risk_Features srf\n        ON ac.Actv_Id = srf.Actv_Id\n       AND TRIM(ac.Ctry_Cd_Iso3) = TRIM(srf.Ctry_Cd_Iso3)\n    WHERE {where_clause}\n    ORDER BY pr.Peak_Sales_Potential DESC\n    \"\"\"\n\n    results = execute_query(query)\n    return jsonify({'submissions': results})\n\n\n@app.route('/api/interventions', methods=['GET'])\n@handle_errors\ndef get_interventions():\n    query = \"\"\"\n    SELECT TOP 20\n        Actv_Id                          AS Protocol_ID,\n        Prod_Brnd_Nm                     AS Product,\n        Ctry_Cd_Iso3                     AS Country_Code,\n        Actv_Sectr                       AS Sector,\n        Regulatory_Authority,\n        CRL_Received_Fl,\n        CRL_Category,\n        GMP_Site_Status,\n        CMC_Readiness_Score,\n        Dose_Optimization_Status,\n        Open_Deficiencies_Cnt,\n        Days_Overdue,\n        Risk_Score_Overall               AS Risk_Score,\n        Recommended_Action,\n        Priority_Score,\n        Peak_Sales_Potential / 1000000.0 AS Revenue_At_Risk_Millions\n    FROM hcls.V_Intervention_Queue\n    ORDER BY Priority_Score DESC\n    \"\"\"\n    results = execute_query(query)\n    return jsonify({'interventions': results})\n\n\n@app.route('/api/regional-breakdown', methods=['GET'])\n@handle_errors\ndef get_regional_breakdown():\n    query = \"\"\"\n    SELECT\n        ac.Global_Region,\n        COUNT(*)                                                          AS submission_count,\n        COUNT(CASE WHEN ac.Actv_Ctry_Sts = 'Approved' THEN 1 END)        AS approved_count,\n        COUNT(CASE WHEN ac.Actv_Ctry_Sts NOT IN ('Approved','Withdrawn') THEN 1 END) AS active_count,\n        SUM(pr.Peak_Sales_Potential) / 1000000000.0                      AS pipeline_billions,\n        SUM(CASE WHEN ac.Actv_Ctry_Sts <> 'Approved'\n                  AND ac.Helth_Auth_Init_Appr_Exptd IS NOT NULL\n                  AND ac.Helth_Auth_Init_Appr_Exptd < CURRENT_DATE\n             THEN pr.Peak_Sales_Potential ELSE 0 END) / 1000000.0        AS revenue_at_risk_millions,\n        AVG(CAST(srf.Risk_Score_Overall AS FLOAT))                        AS avg_risk_score,\n        SUM(pr.Peak_Sales_Potential * 30.0 / 365.0) / 1000000.0         AS accel_30d_millions,\n        SUM(pr.Peak_Sales_Potential * 60.0 / 365.0) / 1000000.0         AS accel_60d_millions,\n        SUM(pr.Peak_Sales_Potential * 90.0 / 365.0) / 1000000.0         AS accel_90d_millions\n    FROM hcls.Activity_Country ac\n    JOIN hcls.Product_Registration pr\n        ON ac.Actv_Id = pr.Actv_Id\n       AND TRIM(ac.Ctry_Cd_Iso3) = TRIM(pr.Ctry_Cd_Iso3)\n    LEFT JOIN hcls.Submission_Risk_Features srf\n        ON ac.Actv_Id = srf.Actv_Id\n       AND TRIM(ac.Ctry_Cd_Iso3) = TRIM(srf.Ctry_Cd_Iso3)\n    GROUP BY ac.Global_Region\n    ORDER BY pipeline_billions DESC\n    \"\"\"\n    results = execute_query(query)\n    return jsonify({'regions': results})\n\n\n@app.route('/api/roi-scenarios', methods=['GET'])\n@handle_errors\ndef get_roi_scenarios():\n    region = request.args.get('region', 'all')\n    sector = request.args.get('sector', 'all')\n\n    where_conditions = [\"Revenue_At_Risk_USD > 0\"]\n    if region != 'all':\n        where_conditions.append(f\"Global_Region = '{region}'\")\n    if sector != 'all':\n        where_conditions.append(f\"Sector = '{sector}'\")\n    where_clause = \" AND \".join(where_conditions)\n\n    query = f\"\"\"\n    SELECT\n        SUM(Value_30Day_Accel_USD) / 1000000.0  AS accel_30d_millions,\n        SUM(Value_60Day_Accel_USD) / 1000000.0  AS accel_60d_millions,\n        SUM(Value_90Day_Accel_USD) / 1000000.0  AS accel_90d_millions,\n        SUM(Revenue_At_Risk_USD) / 1000000.0    AS total_revenue_at_risk_millions,\n        SUM(CRL_Revenue_Impact_USD) / 1000000.0 AS crl_revenue_impact_millions\n    FROM hcls.V_ROI_Calculator\n    WHERE {where_clause}\n    \"\"\"\n\n    results = execute_query(query)\n    if results:\n        r = results[0]\n        return jsonify({\n            'accel30Days': round(float(r['accel_30d_millions'] or 0), 1),\n            'accel60Days': round(float(r['accel_60d_millions'] or 0), 1),\n            'accel90Days': round(float(r['accel_90d_millions'] or 0), 1),\n            'totalRevenueAtRisk': round(float(r['total_revenue_at_risk_millions'] or 0), 1),\n            'crlRevenueImpact': round(float(r['crl_revenue_impact_millions'] or 0), 1),\n        })\n\n    return jsonify({'error': 'No data'}), 404\n\n\n@app.route('/api/submission-details/<actv_id>/<country_code>', methods=['GET'])\n@handle_errors\ndef get_submission_details(actv_id, country_code):\n    query = \"\"\"\n    SELECT\n        ac.Actv_Id                       AS Protocol_ID,\n        pr.Prod_Brnd_Nm                  AS Product,\n        pr.Prod_Typ                      AS Product_Type,\n        ac.Ctry_Name                     AS Country,\n        ac.Ctry_Cd_Iso3                  AS Country_Code,\n        ac.Global_Region,\n        pr.Actv_Sectr                    AS Sector,\n        ac.Regulatory_Authority,\n        ac.Submission_Type,\n        ac.Helth_Auth_Init_Sbmn_Actl     AS Submission_Date,\n        ac.Helth_Auth_Init_Sbmn_Sts      AS Submission_Status,\n        ac.Helth_Auth_Init_Appr_Exptd    AS Expected_Approval_Date,\n        ac.Helth_Auth_Init_Appr_Actl     AS Actual_Approval_Date,\n        ac.Clock_Stop_Days,\n        CASE\n            WHEN ac.Actv_Ctry_Sts = 'Approved' AND ac.Helth_Auth_Init_Appr_Actl IS NOT NULL\n                THEN CAST((ac.Helth_Auth_Init_Appr_Actl - ac.Helth_Auth_Init_Sbmn_Actl) AS INTEGER)\n            ELSE CAST((CURRENT_DATE - ac.Helth_Auth_Init_Sbmn_Actl) AS INTEGER)\n        END AS Days_In_Review,\n        CASE\n            WHEN ac.Actv_Ctry_Sts <> 'Approved' AND ac.Helth_Auth_Init_Appr_Exptd IS NOT NULL\n                THEN CAST((CURRENT_DATE - ac.Helth_Auth_Init_Appr_Exptd) AS INTEGER)\n            ELSE 0\n        END AS Days_Overdue,\n        ac.CRL_Received_Fl,\n        ac.CRL_Received_Dt,\n        ac.CRL_Category,\n        ac.Review_Pathway,\n        ac.Resubmission_Cycle,\n        ac.Project_Orbis_Fl,\n        ac.Parallel_Scientific_Advice_Fl,\n        ac.Num_Of_Sites,\n        ac.Num_Of_Subjs_Exptd,\n        ac.Num_Of_Subjs_Enrld,\n        pr.Peak_Sales_Potential / 1000000.0 AS Peak_Sales_Millions,\n        pr.Rglt_Appr_By_Ctry             AS Regulatory_Approval_Status,\n        pr.Mktg_Sts_By_Ctry              AS Marketing_Status,\n        pr.Blgc_Prod_In                  AS Is_Biologic,\n        srf.Risk_Score_Overall,\n        srf.CMC_Readiness_Score,\n        srf.GMP_Site_Status,\n        srf.Dose_Optimization_Status,\n        srf.Stability_Data_Status,\n        srf.Clinical_Data_Gap_Type\n    FROM hcls.Activity_Country ac\n    JOIN hcls.Product_Registration pr\n        ON ac.Actv_Id = pr.Actv_Id\n       AND TRIM(ac.Ctry_Cd_Iso3) = TRIM(pr.Ctry_Cd_Iso3)\n    LEFT JOIN hcls.Submission_Risk_Features srf\n        ON ac.Actv_Id = srf.Actv_Id\n    WHERE ac.Actv_Id = ?\n    AND TRIM(ac.Ctry_Cd_Iso3) = ?\n    \"\"\"\n\n    results = execute_query(query, (actv_id, country_code.strip()))\n\n    if results:\n        return jsonify({'submission': results[0]})\n\n    return jsonify({'error': 'Submission not found'}), 404\n\n\n@app.route('/api/critical-delays', methods=['GET'])\n@handle_errors\ndef get_critical_delays():\n    query = \"\"\"\n    SELECT\n        Actv_Id                          AS Protocol_ID,\n        Prod_Brnd_Nm                     AS Product,\n        Ctry_Name                        AS Country,\n        Ctry_Cd_Iso3                     AS Country_Code,\n        Actv_Sectr                       AS Sector,\n        Regulatory_Authority,\n        Actv_Ctry_Sts                    AS Status,\n        Days_Overdue,\n        Risk_Score_Overall               AS Risk_Score,\n        Primary_Delay_Driver,\n        CRL_Received_Fl,\n        CRL_Category,\n        GMP_Site_Status,\n        CMC_Readiness_Score,\n        Clock_Stop_Days,\n        Open_Deficiencies_Cnt,\n        Resubmission_Cycle,\n        Review_Pathway,\n        Module3_CMC_Complete_Fl,\n        Module5_Clinical_Complete_Fl,\n        Dose_Optimization_Status,\n        Stability_Data_Status,\n        Clinical_Data_Gap_Type,\n        Peak_Sales_Potential / 1000000.0 AS Peak_Sales_Millions\n    FROM hcls.V_Critical_Delays\n    ORDER BY Days_Overdue DESC, Risk_Score_Overall DESC\n    \"\"\"\n    results = execute_query(query)\n    return jsonify({'criticalDelays': results})\n\n\n@app.route('/api/approval-pipeline', methods=['GET'])\n@handle_errors\ndef get_approval_pipeline():\n    region = request.args.get('region', 'all')\n    sector = request.args.get('sector', 'all')\n\n    where_conditions = [\"1=1\"]\n    if region != 'all':\n        where_conditions.append(f\"Global_Region = '{region}'\")\n    if sector != 'all':\n        where_conditions.append(f\"Sector = '{sector}'\")\n    where_clause = \" AND \".join(where_conditions)\n\n    query = f\"\"\"\n    SELECT\n        Global_Region,\n        Sector,\n        Regulatory_Authority,\n        Review_Pathway,\n        Status,\n        Submission_Count,\n        CRL_Count,\n        Avg_Clock_Stop_Days,\n        Pipeline_Value_USD / 1000000.0  AS Pipeline_Value_Millions\n    FROM hcls.V_Approval_Pipeline\n    WHERE {where_clause}\n    ORDER BY Pipeline_Value_USD DESC\n    \"\"\"\n    results = execute_query(query)\n    return jsonify({'pipeline': results})\n\n\n@app.route('/api/portfolio-summary', methods=['GET'])\n@handle_errors\ndef get_portfolio_summary():\n    query = \"\"\"\n    SELECT\n        Sector,\n        Protocols,\n        Total_Submissions,\n        Approved,\n        CRLs_Received,\n        High_Risk,\n        Orbis_Submissions,\n        Avg_CMC_Readiness,\n        Avg_Enrollment_Pct,\n        Total_Pipeline_USD / 1000000000.0 AS Pipeline_Billions\n    FROM hcls.V_Portfolio_Summary\n    ORDER BY Total_Pipeline_USD DESC\n    \"\"\"\n    results = execute_query(query)\n    return jsonify({'portfolio': results})\n\n\n# ============================================\n# AI RECOMMENDATION ENDPOINT\n# ============================================\n\n@app.route('/api/ai/intervention-recommendation', methods=['POST'])\n@handle_errors\ndef get_intervention_recommendation():\n    \"\"\"\n    Generate an AI recommendation for a specific intervention queue item.\n\n    Request body (JSON):\n        { \"intervention\": { ...row from V_Intervention_Queue... } }\n\n    Returns:\n        { \"recommendation\": {...}, \"model\": \"claude-sonnet-4-5\", \"protocol_id\": \"...\" }\n    \"\"\"\n    body = request.get_json()\n    if not body or 'intervention' not in body:\n        return jsonify({'error': 'Request body must include an \"intervention\" object'}), 400\n\n    item = body['intervention']\n    protocol_id = item.get('Protocol_ID', 'unknown')\n\n    # Guardrail: keyword blocklist (defense-in-depth)\n    input_text = json.dumps(item)\n    is_valid, reason = validate_ai_input(input_text)\n    if not is_valid:\n        return jsonify({'error': f'Input validation failed: {reason}'}), 400\n\n    # Fetch portfolio context for relative urgency framing\n    try:\n        portfolio_query = \"\"\"\n        SELECT TOP 6\n            Actv_Id                          AS Protocol_ID,\n            Prod_Brnd_Nm                     AS Product,\n            Ctry_Cd_Iso3                     AS Country_Code,\n            Regulatory_Authority,\n            Risk_Score_Overall               AS Risk_Score,\n            Days_Overdue,\n            Priority_Score,\n            Peak_Sales_Potential / 1000000.0 AS Revenue_At_Risk_Millions\n        FROM hcls.V_Intervention_Queue\n        ORDER BY Priority_Score DESC\n        \"\"\"\n        portfolio_context = execute_query(portfolio_query)\n    except Exception as e:\n        print(f\"Warning: Could not fetch portfolio context: {e}\")\n        portfolio_context = []\n\n    # Build prompt\n    user_prompt = build_intervention_user_prompt(item, portfolio_context)\n\n    # Call AWS Bedrock\n    try:\n        bedrock = get_bedrock_client()\n\n        response = bedrock.invoke_model(\n            modelId=BEDROCK_CONFIG['model_id'],\n            contentType='application/json',\n            accept='application/json',\n            body=json.dumps({\n                \"anthropic_version\": \"bedrock-2023-05-31\",\n                \"max_tokens\": 1500,\n                \"system\": INTERVENTION_SYSTEM_PROMPT,\n                \"messages\": [{\"role\": \"user\", \"content\": user_prompt}]\n            })\n        )\n\n        response_body = json.loads(response['body'].read())\n        raw_text = response_body['content'][0]['text'].strip()\n\n        # Strip markdown fences if model adds them despite instructions\n        if raw_text.startswith('```'):\n            raw_text = raw_text.split('```')[1]\n            if raw_text.startswith('json'):\n                raw_text = raw_text[4:]\n        raw_text = raw_text.strip()\n\n        recommendation = json.loads(raw_text)\n\n    except json.JSONDecodeError as e:\n        return jsonify({\n            'error': 'AI response was not valid JSON',\n            'raw_response': raw_text if 'raw_text' in locals() else None,\n            'detail': str(e)\n        }), 502\n    except Exception as e:\n        err = str(e)\n        if 'AccessDenied' in err or 'UnauthorizedAccess' in err:\n            return jsonify({'error': 'Bedrock access denied — check AWS credentials and model access in .env'}), 403\n        return jsonify({'error': f'Bedrock call failed: {err}'}), 502\n\n    return jsonify({\n        'recommendation': recommendation,\n        'model': 'claude-sonnet-4-5',\n        'protocol_id': protocol_id,\n    })\n\n\nif __name__ == '__main__':\n    app.run(debug=True, host='0.0.0.0', port=5123)\n